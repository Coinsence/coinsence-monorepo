{
  "name": "coinsence-monorepo",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "postinstall": "npm run bootstrap"
  },
  "dependencies": {
    "@aragon/cli": "^5.4.0"
  }
}
